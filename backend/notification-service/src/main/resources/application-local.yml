spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/clinic_db?currentSchema=notification
    username: postgres
    password: 0000
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

app:
  notification:
    sms:
      endpoint: http://localhost:9091/messages
      api-key: local-dev-token
      sender: Clinic
  security:
    jwt:
      secret: 0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF0123456789ABCDEF
