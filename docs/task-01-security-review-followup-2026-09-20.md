# Task 01 — Security & Booking Integrity: review follow-up

Date: 2026-09-20 (Asia/Bangkok). Workspace `0b5f260d-5bb9-4508-9999-088885fef174` (`D:\MainDV\Clinic-management`); task `18080da3-35e1-4b34-afe9-f6ae890818a6` confirmed by Workbench status. Task remains OPEN.

## Changes made in this follow-up

- `backend/identity-service/src/main/java/com/clinic/identity/security/JwtService.java`: purpose-specific JWT checks fail closed even on malformed claim types, without changing claim names or role format.
- `backend/identity-service/src/main/java/com/clinic/identity/service/AuthService.java`: logout validates refresh purpose and JWT subject against stored token owner; access JWT cannot substitute for logout. Existing refresh rotation uses a row lock and checks `token_type=refresh`.
- `backend/identity-service/src/test/java/com/clinic/identity/service/AuthServiceRefreshTest.java`: access-as-logout and wrong-owner logout regression cases.
- `backend/appointment-service/src/main/java/com/clinic/appointment/security/IdentityAccountClient.java` (new): checks `/api/users/me` using the supplied access JWT on each request; requires identical subject, ACTIVE status and unchanged role set; rejects errors and identity outages. Request timeout 1.5 seconds for connection/read; clone RestClient builder to avoid side effects on other clients. Defaults to `http://identity-service:8083`, configurable using `SERVICES_IDENTITY_URL` / `services.identity.url` for host execution.
- `backend/appointment-service/src/main/java/com/clinic/appointment/security/JwtAuthenticationFilter.java`: uses authoritative account checks, rejects refresh and malformed JWTs, fails closed on changed roles/locked accounts/unavailable identity; filter chain executes once.
- `backend/appointment-service/src/test/java/com/clinic/appointment/security/{IdentityAccountClientTest,JwtAuthenticationFilterTest}.java`: HTTP mock tests for ACTIVE, LOCKED, changed role, mismatched subject, HTTP 401 and bearer filter fail-closed cases.
- `backend/appointment-service/src/main/java/com/clinic/appointment/service/impl/AppointmentServiceImpl.java`: correct permission union when account has both PATIENT and DOCTOR roles; patient can view own booking without requiring assigned-doctor status.
- `backend/appointment-service/src/test/java/com/clinic/appointment/service/impl/AppointmentAuthorizationTest.java`: add multi-role patient, admin view/complete and cross-patient cancel tests.
- `backend/appointment-service/src/test/java/com/clinic/appointment/service/impl/AppointmentConcurrencyIntegrationTest.java`: remove unsafe `appointments.deleteAll()` cleanup, add adjacent-slot PostgreSQL regression. Uses random doctor UUIDs; test records remain in the dedicated DB. This test does NOT run against application database.
- `docs/api-contract.md`: purpose checks, logout behavior, appointment fresh authorization, deployment and cross-service limitations.

Existing Task 01 changes reviewed without undoing them: `appointment/V2__prevent_overlapping_appointments.sql` adds `btree_gist`, CHECK(start_time < end_time), partial GiST exclusion on doctor UUID and `tsrange(date+start,date+end,'[)')` where status is not CANCELLED. Adjacency is mathematically permitted; cancellation frees the slot; SQLSTATE 23P01 translates to HTTP 409 in legacy appointment create. Existing authorization unit tests cover assigned doctor/owning patient and receptionist/admin policy; mutations lock appointment row.

## Actual validation in THIS follow-up

Command from `backend`: `mvn -pl identity-service,doctor-service,appointment-service -am test` — **BUILD SUCCESS** on 2026-09-20 02:25 Asia/Bangkok. Identity: 14 total / 13 executed / 1 skipped; doctor: 14/14; appointment: 47 total / 44 executed / 3 skipped. **75 discovered, 71 executed and passed, 4 skipped; 0 failures and 0 errors.** `git diff --check` returned exit 0 (only unrelated LF/CRLF warnings).

Skipped tests: identity `AuthRefreshConcurrencyIntegrationTest.concurrentRefreshRequestsCannotReuseSameRefreshToken` (1); appointment `AppointmentConcurrencyIntegrationTest` concurrent overlapping bookings, cancelled-slot reuse, and newly added adjacent bookings (3). Both classes require `CLINIC_SECURITY_TEST_DB=true` and hard-code dedicated database `clinic_security_test` at `localhost:5432` with Spring Boot/Flyway. Without the flag the classes are intentionally skipped.

Environment checks this turn: Docker CLI could not reach `npipe:////./pipe/dockerDesktopLinuxEngine`; TCP `127.0.0.1:5432` reported False. A Windows PostgreSQL service is running and port 5433 is open, but the read-only database inspection command was denied by the tool safety check. **Do not redirect tests to port 5433**, since database identity and isolation could not be established; do not set the opt-in flag. No DB destructive command or live migration was run in this follow-up. An earlier Task 01 run on Sep 20 around 00:25 had verified the pre-existing two appointment concurrency tests and identity refresh concurrency against an isolated PostgreSQL 16 `clinic_security_test`; that earlier result does not verify today's newly added adjacency test or the new identity freshness client.

## Task 03 coordination and remaining release blockers

Coordination messages exchanged: Task 03 confirmed its new Reception/Queue/V3 files are in a **separate worktree**, while Task 01 owns the existing `AppointmentServiceImpl`, `AppointmentController`, `AppointmentRepository`, JWT filter and V2 migration. Task 03 will not overwrite those files; Task 01 has not edited its `DoctorClient`, `PatientClient`, `GlobalExceptionHandler`, V3 or Reception classes. Task 03 implemented `ReceptionQueueService.completeCheckedInAppointment(...)` in its worktree; **the class is not present in Task 01's checkout**. Do not import it until integration. Once integrated, legacy complete must call this bridge under appointment row lock; legacy cancel must prohibit cancellation if ReceptionVisit exists after check-in; otherwise queue/appointment status can diverge. Task 03 staff booking/reschedule must use V2 constraint and map 23P01 to 409; its BookingDayLock should not be removed until all paths are verified. No merge or code transfer occurred.

Remaining cross-service revocation gap: identity checks current status/roles, and appointment does a synchronous identity check per access request. Patient, medical-record, billing (and notification pending separate review) have JWT bearer filters that still rely on signature/embedded role claims. Stale access tokens can retain privileges until expiry (currently 60 minutes), and some filters may treat refresh JWTs as bearer tokens. Global immediate revocation requires coordinated rollout of the same fail-closed identity check or a shared introspection/revocation mechanism to each service, with direct-port and service-to-service compatibility tests. Do NOT claim global revocation, shorten token lifetime blindly, change JWT contract everywhere or force merged changes onto Task 03's branch. Existing access tokens lacking `token_type` also require re-login on deployment of earlier Task 01 purpose checks.

Do not mark Task 01 complete before a dedicated PostgreSQL test run validates new adjacency, migration on intended target is separately checked, Task 03 queue bridge is integrated, and cross-service revocation gap is assigned and reviewed.

No `reset`, `restore`, `stash`, `clean`, `stage`, `commit`, `push` or merge of anyone else's changes was performed.
